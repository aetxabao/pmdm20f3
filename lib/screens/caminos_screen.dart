import 'package:flutter/material.dart';

class CaminosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Caminos de Santiago"),
      ),
      body: Text("Esto deberían ser dos RaisedButton centrados"),
    );
  }
}
